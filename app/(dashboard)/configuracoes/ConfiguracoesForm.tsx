"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {Building2,CheckCircle2,Save,Settings2} from "lucide-react";
export default function ConfiguracoesForm({companyName}:{companyName:string}){const router=useRouter();const[name,setName]=useState(companyName),[saving,setSaving]=useState(false),[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();setSaving(true);setMessage("");const r=await fetch("/api/configuracoes",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({companyName:name})});const d=await r.json();setSaving(false);if(!r.ok){setMessage(d?.error||"Não foi possível salvar.");return}setMessage("Configurações salvas com sucesso.");router.refresh()}return <div className="space-y-6"><header><p className="eyebrow">Sistema</p><h2 className="page-title">Configurações</h2><p className="page-subtitle">Personalize as informações principais da sua operação.</p></header><section className="card max-w-2xl p-5 sm:p-6"><div className="mb-6 flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-slate-100"><Settings2 size={20}/></div><div><h3 className="section-title">Identidade da empresa</h3><p className="text-sm text-slate-500">Esse nome aparece no menu do sistema.</p></div></div><form onSubmit={submit}><label className="block space-y-1.5 text-sm font-semibold"><span className="flex items-center gap-2"><Building2 size={15}/>Nome da empresa</span><input className="input" value={name} onChange={e=>setName(e.target.value)} required/></label><div className="mt-5 flex items-center gap-3"><button className="btn btn-primary" disabled={saving}>{saving?"Salvando...":<><Save size={16}/>Salvar configurações</>}</button>{message&&<span className="flex items-center gap-1.5 text-sm font-semibold text-emerald-700"><CheckCircle2 size={16}/>{message}</span>}</div></form></section></div>}
